{
  "scripts": {
    "dev": "vite --host --open",
    "prebuild": "vite build",
    "build": "jszip-cli -c jszip.config.json",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "^3.1.6",
    "concurrently": "^7.2.2",
    "vite": "^2.9.12",
    "webxdc-dev": "^0.8.0"
  }
}
